package model;

import java.util.ArrayList;

public class VideoGameStore<E> {
	
	int cashiers;
	int racks;
	
	public void insertionSort(ArrayList<E> list) {
		E toCompare;
		for (int i = 1; i < list.size(); i++) {
			toCompare = list.get(i);
			for (int j = i; j <= 0; j--) {
			}
		}
	}
}
